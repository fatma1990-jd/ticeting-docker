spring:
  application:
    name: user-service
  profiles:
    active: user_service
  cloud:
    config:
      name: user-service,config-client
      uri: http://configserver:8888
      fail-fast: true
      retry:
        maxAttempts: 200
        maxInterval: 10000
